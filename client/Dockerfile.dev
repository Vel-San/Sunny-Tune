FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
# Run unit tests at image-build time — image won't be created if tests fail
RUN npm test
EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
