/**
 * @fileoverview Global test setup for the client test suite.
 *
 * Imported by vitest before each test file via `setupFiles` in vitest.config.ts.
 * Configures @testing-library/jest-dom custom matchers and any global mocks.
 */

import "@testing-library/jest-dom";
